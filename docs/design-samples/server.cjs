const http=require('http'),fs=require('fs'),p=require('path');const ext={'.html':'text/html'};http.createServer((q,r)=>{let f=p.join(__dirname,decodeURIComponent(q.url.split('?')[0]));if(q.url==='/'||q.url==='')f=p.join(__dirname,'sample1-vertical-feed.html');fs.readFile(f,(e,d)=>{if(e){r.writeHead(404);r.end('nf')}else{r.writeHead(200,{'content-type':ext[p.extname(f)]||'text/plain'});r.end(d)}})}).listen(8731,()=>console.log('up'))
